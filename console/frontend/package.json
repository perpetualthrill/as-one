{
  "name": "as-one-frontend",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "es6-promise": "^4.2.6",
    "isomorphic-fetch": "^2.2.1",
    "react": "^16.8.6",
    "react-dom": "^16.8.6",
    "react-scripts": "3.0.1"
  },
  "scripts": {
    "start": "react-scripts start",
    "build": "react-scripts build",
    "test": "react-scripts test",
    "eject": "react-scripts eject",
    "standardize": "standard --fix",
    "install": "react-scripts build; rm -r ../backend/src/main/resources/web-static/*; cp -r ./build/* ../backend/src/main/resources/web-static/"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "eslint": "^5.16.0",
    "eslint-plugin-react": "^7.13.0",
    "standard": "^12.0.1"
  },
  "standard": {
    "env": {
      "mocha": true
    }
  },
  "proxy": "http://localhost:12345/"
}
